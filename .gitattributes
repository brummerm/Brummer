# Default: normalize to LF on checkout for everything
* text=auto

# Shell scripts must stay LF on all platforms — CRLF breaks bash on Linux
*.sh text eol=lf

# Windows-only files can keep CRLF
*.bat text eol=crlf
